{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend server for evently",
  "main": "server.js",
  "scripts": {
    "start": "json-server -p 8080 -w ./backend/data/events.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "json-server": "^0.17.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
